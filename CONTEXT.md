# Project Context: Steam Regional Popularity Heatmap

A web app showing, per country/region, which Steam games are most disproportionately popular there — using review-language distribution as a proxy for geography, since Steam has no public per-country data.

Read this file in full at the start of any session touching this project. Read individual ADRs in `adr/` only when working in the area they concern. Follow the development process in `docs/methodology.md` — strict TDD cycles, one commit per cycle, push per feature.

## Domain vocabulary

- **Region** — a country or group of countries inferred from a Steam review-language code. Most languages map to one country (e.g. `japanese` → Japan); some map to a blended group (e.g. `english` → US/UK/Canada/Australia/etc., merged and disclosed as such). See ADR-002.
- **Language-as-proxy** — the core data strategy: Steam review language distribution stands in for true per-country data, which Steam does not expose publicly. See ADR-001.
- **Concentration score** — how disproportionately popular a game is in a region, relative to that region's average share across all tracked games (not raw review volume). See ADR-003.
- **Wilson score (lower bound)** — the statistical adjustment applied to concentration scores so that small review samples don't produce misleadingly extreme rankings. Confidence level finalized at 95%. See ADR-004.
- **Ranking eligibility** — a game must have at least 50 reviews in a region's language to be ranked in that region; sub-threshold games still count toward the region's baseline. Protects against confident-but-tiny shares divided by tinier baselines, which Wilson cannot catch. See ADR-013.
- **Featured game** — the current global #1 by most-played rank; its heatmap is the map's landing state. The map always encodes one game at a time — see ADR-014.
- **Top-N games** — the fixed seed set (current top ~100 games by live Steam concurrent-player count) that the daily pipeline tracks. Not the full Steam catalog. See ADR-005.
- **Daily job** — the single unified batch process (Python) that fetches top-N rankings and per-language review counts once per day, computes scores, and writes results to Postgres.

## Architecture at a glance

- **Web app**: C# / ASP.NET Core MVC, single project with folder-based (not project-based) separation between domain / infrastructure / web layers. Razor views + plain CSS + D3.js for the map visualization only.
- **Batch/analysis job**: Python, runs daily via GitHub Actions (scheduled workflow), independent of wherever the web app is hosted.
- **Integration between C# and Python**: shared Postgres database only (Supabase, free tier). No direct process-to-process coupling — see ADR-006.
- **Data source**: Steam public Web API. `ISteamChartsService`/`GetMostPlayedGames` for rankings; `appreviews` (with `num_per_page=0`, once per language code) for language breakdown. ~29 calls/game/day, well under the 100k/day API cap.
- **Hosting**: Azure App Service (F1, free tier) for the web app; Supabase for Postgres; GitHub Actions for both scheduling and CI. VPS migration planned later, deliberately deferred (see ADR-007).
- **Testing**: TDD on domain-level logic only (Wilson scoring, region mapping, aggregation). Integration/e2e tests deferred until needed. `ISteamClient` (C#) / `SteamClient` protocol (Python) is the seam faked in tests — see the `tdd` skill's `mocking.md`.
- **Repo**: single monorepo, `/web` (C#) and `/analysis` (Python) side by side, single shared documentation.

## Current status

As of 2026-08-14 — MVP fully working end to end, redesigned map, visual pass shipped, out-of-catalog search shipped. **All issues closed** (#1 and its full tree, including #22's sub-issues).

- **Done** (issues #2–#11, #14 closed): walking skeleton; daily pipeline tracking the real top-100 most-played games across all 30 review languages (~3,100 keyless API calls/day, runs green on GitHub Actions, retries transient Steam 5xx/429 with backoff); full 30-region language mapping per ADR-002's concrete table; **ranking eligibility** — a game needs 50+ in-language reviews to rank in a region (ADR-013; the Arabic region drops to the grey no-data treatment as a result, so 29 regions currently display); Wilson confidence finalized at 95%; world map rendered from self-hosted Natural Earth GeoJSON, the D3 code living in `web/wwwroot/js/map.js` behind `initRegionMap(...)`; store links; always-visible disclosures; **production hosting** — live at https://steamheatmap.azurewebsites.net (Azure App Service F1 Linux, France Central, resource group `steamheatmap-rg`), deployed by `.github/workflows/deploy-web.yml` on pushes to `main` touching `web/`. The web app is template-free: no Bootstrap/jQuery, plain CSS only (ADR-010 fully realized).
- **Map redesign shipped** (ADR-014, grill-me completed 2026-07-18): the map always shows one game's concentration heatmap — landing on the featured game — replacing the region-level choropleth, whose intensity encoded language-community size rather than insight. Single-hue log ramp, fixed clamped domain [×⅛, ×8] (data-backed); fill semantics ramp/white/gray; blended regions signaled by synchronized member-country highlight + region-titled popups (resolves ADR-002's open mechanism); full dataset embedded in the page, no endpoints.
  - **#10** (per-game heatmap spine): pipeline persists most-played rank, view model exposes tracked games + per-game/per-region concentration lookup, map paints one game's ramp with headline and hover popup.
  - **#11** (selected-mode state machine): click enters selected mode (heatmap clears to white/gray, region gets a persistent highlight, side panel shows the region-titled top 10); clicking another region switches directly; hover-preview still works while selected; all three exit routes (re-click, click empty map space, Esc) restore heatmap mode.
  - **#14** (game search typeahead): client-side substring filter over the embedded, rank-ordered game list; empty input shows the rank-ordered top with the featured game first; selecting a suggestion repaints the heatmap/headline, exits selected mode if active, and clears the input; no-match hint discloses the top-100 dataset boundary.
- **Visual pass shipped** (ADR-015, grill-me 2026-07-22, built 2026-07-31; issues #15–#21 closed): a dark, Steam-inspired identity for the homepage — deep navy page, navy surface, Steam blue accent — plus the layout composition ADR-015 was **amended** to allow. The amendment matters: the ADR originally scoped the pass as restyle-only, and looking at the live page showed the map's lack of weight was structural rather than chromatic, so the decision was revised and #16 re-scoped before any code.
  - **#16** (foundation): `site.css` gains a CSS custom-property token layer, extracted first as a proven no-op (byte-identical render) so the palette change stayed reviewable. Composition: the map is now the page's only framed element and the region panel a plain column beside it; headline, caption and search moved inside the map surface as its header; the row split went to ~3:1; the amber disclosures box dissolved into a muted caption under the headline plus a footer notice — both sentences verbatim and always visible, so #6's requirement still holds.
  - **#21** (map crop, filed mid-pass): Antarctica dropped and the `viewBox` taken from the drawn land's bounds — `0 0 960 500` became `59 0 870 418`. Fiji is excluded from the *crop measurement* only: it straddles the antimeridian, so its bounding box spanned the full width and cost 10.3% of the map's scale. Projection rotation was measured as the non-destructive alternative and rejected — Chukotka and the Aleutians straddle it too, so every central meridian splits something, and the ones sparing Fiji cut Alaska.
  - **#17** (heatmap colors, amends ADR-014's values only): the ramp runs dark→light now, validated rather than eyeballed (single hue, monotone lightness, low end 2.01:1 on the surface; 27 distinct fills spread evenly over real data). `trackedNoSignalFill` and `noDataFill` both went **neutral**, so neither reads as a low rung of the blue ramp — a region with no signal is a different category, not a smaller number.
  - **#18** (typography): self-hosted Manrope, no font request leaves the origin. **One** variable `.woff2`, not the two the ADR assumed — Google's 400 and 700 latin URLs are byte-identical. `OFL.txt` ships beside it. Five-role type scale at 400/700 only.
  - **#19** (loading placeholder) and **#20** (three CSS micro-interactions at 130ms, `prefers-reduced-motion` honored, panel and headline swaps still instant).
- **Out-of-catalog search shipped** (ADR-016, grill-me 2026-08-08; amends ADR-006 and ADR-012): the ability to search and get a live heatmap for any Steam game, not just the daily top-100, while keeping searched games warm in the daily rotation. Tracked under parent issue #22 (sub-issue of #1, now closed), split into five vertical slices #23–#27, all closed.
  - **#23** (persist region baselines): `region_baselines (run_id, region_code, baseline_share)` now written alongside `region_scores` every run — previously `region_baseline_share` lived only in memory. Needed so a later on-demand-fetched game can be scored against the same day's baseline without recomputing it.
  - **#25** (relevance-based tracked-game rotation): `games.last_relevant_at`, bumped whenever a game appears in a real top-100 fetch or a user search (#27). The daily job's app_id set becomes `select_app_ids_to_track(top_100, still_relevant)` — a pure, TDD'd union — and `most_played_rank` is explicitly cleared to `NULL` for anything outside today's real top-100. Run history is pruned to the 3 most recent runs after every write.
  - **#24** (weekly Steam catalog cache): `ISteamApps/GetAppList/v2/`, the endpoint this was originally built against, turned out to no longer exist on Steam's live API (confirmed 404). Its replacement, `IStoreService/GetAppList/v1/`, requires a Steam Web API key — the project's first, breaking ADR-012's "no key needed" premise for exactly one job — and is paginated (50,000 apps/page via `last_appid`/`have_more_results`) rather than single-shot. The developer registered a free key (steamcommunity.com/dev/apikey); `RequestsSteamClient.get_all_apps()` now loops pages, ADR-012 is amended with the confirmed live mechanics. Live-verified: 179,180 apps fetched and written to `steam_apps`.
  - **#26** (full-catalog search UI): `POST`-free `GET /Home/SearchCatalog?q=` queries `steam_apps` by substring (ILIKE, DB-side), ranked exact-match > prefix > mid-string and capped at 10 by `CatalogSearchViewModelBuilder` (tested; the Postgres repository is untested by design, ADR-008). `map.js` keeps #14's instant local-dataset filter as the first thing shown, then supplements it with a 250ms-debounced call to the new endpoint only once local coverage runs thin, deduped against what's already shown. Headless-verified against the live app.
  - **#27** (on-demand live fetch for untracked games): selecting an untracked result now does something. `OnDemandScoring` is a bit-exact C# port of `analysis/steamheatmap/scoring.py`'s Wilson/concentration math (verified against the same golden test values — `region_baseline_share` is deliberately *not* ported, since on-demand scoring always reads a baseline #23 already persisted). `OnDemandGameFetcher` fires ~29 Steam calls concurrently (`Task.WhenAll`), applies the same 50-review ranking floor as the daily pipeline (ADR-013), and — only if at least one region clears it — upserts (not inserts, to survive a retry or a near-simultaneous duplicate search) into `games`/`region_scores` under the *current* run_id, so the result is visible to every visitor immediately. `map.js`'s clicked result shows its own loading/error state and repaints the map with no page reload. Headless- and DB-verified end to end against the live app: Portal 2 (28/29 regions scored, map fill genuinely repainted, not just the headline text), a real too-few-reviews game showing the honest message and staying clickable to retry, and no regression to an ordinary already-tracked click.
- Deferred polish noted on #11 (not filed as issues): blended regions hard to see as a unit pre-hover; friendlier visual form for ×values. Architecture-review candidates deliberately deferred: deepen the daily-job composition (`run_daily_job` behind one seam), name-based column reads in `PostgresRankingRepository`. Carried forward from the visual pass, none filed: a failed GeoJSON fetch leaves "Loading map…" up indefinitely (better than the blank box it replaced, still misleading — no error state was ever scoped); ADR-015 self-hosted the font to avoid a third-party runtime request, but the page still loads D3 from jsDelivr per ADR-010, so that goal is half-met; the search dropdown fades in but not out, because `map.js` dismisses it by clearing `innerHTML` — #26/#27 didn't touch this, still open; and the deferred responsive pass now inherits a map header bar sharing a row between headline and search, which it will need to stack. New from #27: an on-demand-fetched game's own heatmap works everywhere immediately, but it won't appear in *other* regions' top-10 side-panel lists until the next daily pipeline run recomputes those globally — intended per ADR-016, not a bug, but worth remembering if it looks like a gap.
- The daily pipeline and web app need no Steam API key — `GetMostPlayedGames` and `appreviews` are both public. `STEAM_API_KEY` exists now (GitHub Actions secret + local user env var) but is scoped to exactly one job: the weekly catalog refresh's `get_all_apps()`. Secrets: `SUPABASE_DB_URL` (GitHub Actions secret + local user env var + Azure app setting — update all three together when rotating, and prefer alphanumeric-only passwords: a special-char rotation once produced a pooler credential only the newest client libraries could authenticate against), `AZURE_WEBAPP_PUBLISH_PROFILE` (GitHub Actions secret for the deploy workflow), and `STEAM_API_KEY` (GitHub Actions secret + local user env var, weekly-catalog-refresh.yml only).
- **Azure hosting is on a 30-day trial as of 2026-08-14** (the prior free-tier setup stopped being sufficient) — the developer doesn't want to pay long-term, so hosting needs revisiting before the trial lapses (~2026-09-13). ADR-007's "VPS migration deliberately deferred" was written assuming free-tier hosting indefinitely; that premise no longer holds.
